package org.example.model;

public interface AccionesNocturnas {
    void registrar(AccionVotoMafia accion);

    void registrar(AccionInvestigar accion);

    void registrar(AccionProteger accion);
}
